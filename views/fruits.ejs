<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Fruits & Vegetables</title>

  <script src="https://kit.fontawesome.com/5471644867.js" crossorigin="anonymous"></script>
  <link rel="shortcut icon" type="image/jpg" href="/favicon.ico" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

  <style>
    /* Glass-like card */
    .glass-card {
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: saturate(180%) blur(12px);
      -webkit-backdrop-filter: saturate(180%) blur(12px);
      border-radius: 1rem;
      border: 1px solid rgba(209, 213, 219, 0.3);
      box-shadow: 0 4px 15px rgb(0 0 0 / 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .glass-card:hover {
      transform: translateY(-6px) scale(1.03);
      box-shadow: 0 10px 30px rgb(0 0 0 / 0.15);
    }

    /* Badge styling */
    .badge {
      font-size: 0.75rem;
      font-weight: 600;
      padding: 0.25rem 0.6rem;
      border-radius: 9999px;
      text-transform: uppercase;
    }

    .badge-featured {
      background-color: #4f46e5; /* indigo */
      color: white;
    }

    .badge-discount {
      background-color: #dc2626; /* red */
      color: white;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-green-50 to-yellow-50 min-h-screen font-sans text-gray-900">
  <%-  include('./partials/header') %>
  <div class="max-w-7xl mx-auto px-6 py-12">
    <h1
  class="text-5xl font-extrabold mb-12 text-center tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-green-700 to-yellow-600 drop-shadow-md">
  Explore Fruits &amp; Vegetables
</h1>

<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
  <% products.forEach(product => { %>
    <div class="glass-card overflow-hidden flex flex-col">
      <% if (product.isFeatured) { %>
        <div class="absolute top-3 left-3 badge badge-featured shadow-lg select-none z-10" title="Featured Product">
          🌟 Featured
        </div>
      <% } %>
      <% if (product.discount && product.discount > 0) { %>
        <div class="absolute top-3 right-3 badge badge-discount shadow-lg select-none z-10" title="Discount Percentage">
          -<%= product.discount %>% OFF
        </div>
      <% } %>
      <img src="data:image/jpeg;base64,<%= Buffer.from(product.image).toString('base64') %>" alt="<%= product.name %>"
        class="w-full h-64 object-cover rounded-t-lg" loading="lazy" />

      <div class="p-5 flex flex-col flex-grow">
        <h2 class="text-2xl font-semibold mb-2 truncate" title="<%= product.name %>"><%= product.name %></h2>

        <p class="text-gray-700 mb-3 line-clamp-3" title="<%= product.description %>"><%= product.description %></p>

        <div class="flex flex-wrap gap-3 text-sm mb-5 text-gray-600">
          <div><i class="fas fa-tag mr-1 text-green-600"></i>Category: <strong><%= product.category %></strong></div>
          <div><i class="fas fa-boxes mr-1 text-yellow-600"></i>Stock: <%= product.stock %></div>
          <div><i class="fas fa-star mr-1 text-yellow-400"></i>Rating: <%= product.rating %>/5</div>
        </div>

        <div class="text-3xl font-extrabold text-green-700 mb-5 select-none">₹ <%= product.price %></div>

        <div class="mt-auto space-y-3">
          <a href="/addtocart/<%= product._id %>"
            class="block bg-green-700 text-white py-3 rounded-lg font-semibold text-center shadow-md hover:bg-green-800 transition flex justify-center items-center gap-2">
            <i class="fas fa-cart-plus"></i> Add to Cart
          </a>
          <a href="/order?productid=<%= product._id %>"
            class="block bg-yellow-600 text-white py-3 rounded-lg font-semibold text-center shadow-md hover:bg-yellow-700 transition flex justify-center items-center gap-2">
            <i class="fas fa-bolt"></i> Order Now
          </a>
        </div>
      </div>
    </div>
  <% }) %>
</div>
  </div>
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
  AOS.init({
    offset: 120,
    delay: 5,
    duration: 900,
    // once: false,
  });
  
</script>
</body>

</html>
