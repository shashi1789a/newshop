<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Baby Cares</title>
  <script src="https://kit.fontawesome.com/5471644867.js" crossorigin="anonymous"></script>
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* Glassmorphism container */
    .glass-card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: saturate(180%) blur(12px);
      -webkit-backdrop-filter: saturate(180%) blur(12px);
      border-radius: 1.5rem;
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      transition: transform 0.35s ease, box-shadow 0.35s ease;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      height: 100%;
    }

    .glass-card:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 12px 48px 0 rgba(31, 38, 135, 0.6);
      border-color: rgba(255, 255, 255, 0.5);
    }

    /* Keep large image height */
    .glass-card img {
      height: 15rem; /* large image height */
      width: 100%;
      object-fit: cover;
      border-bottom: 1px solid rgba(255, 255, 255, 0.3);
      border-top-left-radius: 1.5rem;
      border-top-right-radius: 1.5rem;
      box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.1);
    }

    /* Content container with reduced padding */
    .content {
      padding: 0.75rem 1rem;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    /* Reduce spacing and clamp description text */
    p.description {
      font-size: 0.875rem;
      color: #4b5563;
      margin-bottom: 0.5rem;
      display: -webkit-box;
      -webkit-line-clamp: 2; /* max 2 lines */
      -webkit-box-orient: vertical;
      overflow: hidden;
      text-overflow: ellipsis;
      max-height: 3rem;
    }

    /* Info styling */
    .info {
      font-size: 0.875rem;
      color: #1f2937;
      margin-bottom: 0.5rem;
      line-height: 1.2;
    }

    /* Price glow with smaller font */
    .price-glow {
      font-size: 1.5rem;
      font-weight: 800;
      color: #4f46e5;
      margin-bottom: 0.5rem;
      user-select: none;
      text-shadow: 0 0 8px #3b82f6, 0 0 20px #2563eb;
      transition: text-shadow 0.3s ease;
    }

    .price-glow:hover {
      text-shadow: 0 0 12px #2563eb, 0 0 28px #1d4ed8, 0 0 40px #1e40af;
    }

    /* Smaller buttons */
    .action-btn {
      padding: 0.5rem 0;
      font-size: 0.875rem;
      border-radius: 1rem;
      font-weight: 600;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
      user-select: none;
    }

    .action-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .add-to-cart {
      background-color: #4f46e5;
      color: white;
    }

    .add-to-cart:hover {
      background-color: #4338ca;
    }

    .order-now {
      background-color: #16a34a;
      color: white;
    }

    .order-now:hover {
      background-color: #15803d;
    }

    /* Wishlist button */
    .wishlist-btn {
      font-size: 0.875rem;
      padding: 0.5rem 0;
      color: #dc2626;
      cursor: pointer;
      user-select: none;
      border-radius: 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      transition: color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
      background: transparent;
      border: none;
      outline: none;
      box-shadow: 0 4px 8px rgba(220, 38, 38, 0.2);
    }

    .wishlist-btn:hover {
      color: #b91c1c;
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(220, 38, 38, 0.3);
    }

    /* Bounce animation for wishlist heart */
    @keyframes bounceHeart {
      0%,
      100% {
        transform: scale(1);
        color: #ef4444;
      }

      50% {
        transform: scale(1.3);
        color: #b91c1c;
      }
    }

    .wishlist-btn.active i {
      animation: bounceHeart 0.6s ease forwards;
    }

    /* Badges */
    .badge {
      position: absolute;
      top: 1rem;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 700;
      color: white;
      user-select: none;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
      z-index: 10;
    }

    .featured-badge {
      left: 1rem;
      background-color: #6366f1;
    }

    .discount-badge {
      right: 1rem;
      background-color: #dc2626;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%,
      100% {
        opacity: 1;
      }

      50% {
        opacity: 0.6;
      }
    }

    /* Tooltip styling */
    .tooltip {
      position: relative;
      cursor: pointer;
    }

    .tooltip:hover::after {
      content: attr(data-tip);
      position: absolute;
      top: -1.75rem;
      left: 50%;
      transform: translateX(-50%);
      background: #111827;
      color: white;
      padding: 0.25rem 0.5rem;
      border-radius: 0.375rem;
      font-size: 0.75rem;
      white-space: nowrap;
      pointer-events: none;
      opacity: 1;
      transition: opacity 0.3s ease;
      z-index: 10;
    }

    .tooltip::after {
      opacity: 0;
      pointer-events: none;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-indigo-50 via-white to-blue-50 min-h-screen font-sans text-gray-900">
    <%-  include('./partials/header') %>
  <div class="max-w-7xl mx-auto px-6 py-12">
    <h1 class="text-5xl font-extrabold mb-16 text-center tracking-tight text-indigo-800 drop-shadow-lg">
      Explore Our Baby Products
    </h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
      <% products.forEach(product => { %>
      <div class="glass-card relative">
        <% if (product.isFeatured) { %>
        <div class="badge featured-badge tooltip" data-tip="Featured Product">🌟 Featured</div>
        <% } %>
        <% if (product.discount > 0) { %>
        <div class="badge discount-badge tooltip" data-tip="Discount">-<%= product.discount %>% OFF</div>
        <% } %>

        <img src="data:image/jpeg;base64,<%= Buffer.from(product.image).toString('base64') %>" alt="<%= product.name %>" loading="lazy" />

        <div class="content">
          <h2 class="text-xl font-extrabold text-indigo-900 truncate mb-1" title="<%= product.name %>"><%= product.name %></h2>
          <p class="description" title="<%= product.description %>"><%= product.description %></p>

          <div class="info">
            <p><i class="fas fa-tag mr-1 text-indigo-600"></i>Category: <%= product.category %></p>
            <p><i class="fas fa-industry mr-1 text-indigo-600"></i>Brand: <%= product.brand %></p>
            <p><i class="fas fa-capsules mr-1 text-indigo-600"></i>Type: <%= product.type %></p>
          </div>

          <div class="price-glow">₹ <%= product.price %></div>

          <div class="flex flex-col gap-2 mt-auto">
            <a href="/addtocart/<%= product._id %>" class="action-btn add-to-cart">
              <i class="fas fa-cart-plus"></i> Add to Cart
            </a>
            <a href="/order?productid=<%= product._id %>" class="action-btn order-now">
              <i class="fas fa-shopping-bag"></i> Order Now
            </a>
            <button class="wishlist-btn" title="Add to Wishlist">
              <i class="fas fa-heart"></i> Wishlist
            </button>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
  </div>
</body>

</html>
